# mock: no external provider credential is read. production: credentials are required for opt-in smoke tests.
APP_PROVIDER_MODE=mock

# Required only for: npm run smoke:providers:production
GEMINI_API_KEY=
GOOGLE_CLOUD_PROJECT_ID=
GOOGLE_CLOUD_ACCESS_TOKEN=
